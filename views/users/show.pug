extends ../layout

block content
  .container
    h1.main-title= users.name
          
    dl.row
      dt.col-sm-3 Email
      dd.col-sm-9
        a(href=`mailto:${users.email}`)= users.email

      dt.col-sm-3 Created At
      dd.col-sm-3=moment(users.createdAt).format('YYYY년 MM월 DD일 HH시 mm분')


    p
      a.btn.btn-outline-secondary(href='/users/') 회원 목록
      | 
      a.btn.btn-outline-primary(href=`/users/${users._id}/edit`) 회원정보 수정
      | 
      a.btn.btn-outline-danger(href=`/users/${users._id}?_method=DELETE`) 회원 탈퇴
      
    br  
    hr
    br  
    h3
      | Admin My Events
    br
    table.table.table-hover
      thead
        tr
          th Title
          th Location
          th Start Time
          th Participant
          th 
          th
          th
      tbody
        each event in events
          tr
            td 
              a(href=`/events/${event._id}`)= event.title
              td= event.location
              td= event.startTime
              td= event.participantN
              td
                a.btn.btn-sm.btn-outline-secondary(href=`/events/${event._id}/edit`) 
                  | #[i.fa.fa-pencil] Edit
                | 
                a.btn.btn-sm.btn-outline-danger(href=`/events/${event._id}?_method=DELETE`)
                  | #[i.fa.fa-trash] Delete
                |
                |
                a.btn.btn-sm.btn-outline-dark(href=`/events/${event.id}/participantL`) Participate Lists
                |
                |
                a.btn.btn-sm.btn-outline-dark(href=`/events/${event.id}/survey`) Survey Result
    br
    hr
    br
    h3
      | My Favorite Events
    br
    table.table.table-hover
      thead
        tr
          th Title
          th Location
          th Start Time
          th Organizer Name
          th Participant
      tbody
        each favorite in favorites
          tr
            td 
              a(href=`/events/${favorite._id}`)= favorite.title
              td= favorite.location
              td= favorite.startTime
              td= favorite.RegisOrgan
              td= favorite.participantN
              
          
    br
    hr
    br
    h3
      | Recommended Events For You
    br
    table.table.table-hover
      thead
        tr
          th Title
          th Location
          th Start Time
          th Organizer Name
          th Participant
      tbody
        each recommend in recommends
          tr
            td 
              a(href=`/events/${recommend._id}`)= recommend.title
              td= recommend.location
              td= recommend.startTime
              td= recommend.RegisOrgan
              td= recommned.participantN
