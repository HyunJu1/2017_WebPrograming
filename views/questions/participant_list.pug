extends ../layout

block content
  .container


    h1.main-title= questions.title
    br
    h4
      | Participant List
    br
    .question.show
      each user in users
        .row
          .col-sm-4
            | Name
            .content= user.name
          .col-sm-4  
            | Email
            .smallText= user.email
    
